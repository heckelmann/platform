apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: helm-deplyments
spec:
  generators:
    - git:
        repoURL: https://github.com/heckelmann/platform.git
        revision: HEAD
        files:
          - path: gitops/helm/**/config.yaml
  template:
    metadata:
      name: '{{path.basename}}'
    spec:
      project: default
      sources:
      - repoURL: '{{ repoURL }}'
        targetRevision: '{{ targetRevision }}'
        chart: '{{ chart }}'
        helm:
          valueFiles:
          - $values/gitops/helm/{{ path.basename }}/values.yaml
      - repoURL: https://github.com/heckelmann/platform.git
        targetRevision: HEAD
        path: gitops/helm/{{ path.basename }}
      - repoURL: https://github.com/heckelmann/platform.git
        targetRevision: HEAD
        ref: values
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{ namespace }}'
      syncPolicy:
        automated: {}
        syncOptions:
          - CreateNamespace=true